[phases.setup]
nixPkgs = ['python310', 'nodejs-18_x']

[phases.install]
cmds = [
    'pip3 install -r backend/requirements.txt',
    'playwright install chromium --with-deps || playwright install chromium || true'
]

[start]
cmd = 'cd backend && python3 api_server.py'
